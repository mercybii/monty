#include "monty.h"

/**
 * nop - it does nothing
 *
 * @stack: pointer to the list
 * @line_number: is the number of line where the commands are found
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
